{% extends 'base.html.twig' %}

{% block content %}
    <h1>Ajout niveau</h1>
    {{ form_start(form) }}

      {{ form_row(form.design, {'attr': {'class': 'form-control', 'placeholder': Désignation du niveau'}}) }}
        
    <br/>
    <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}

{% endblock %}